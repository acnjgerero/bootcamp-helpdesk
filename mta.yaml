_schema-version: 3.3.0
ID: bootcamp-helpdesk
description: A simple CAP project.
version: 1.0.0
modules:
- name: bootcamp-helpdesk-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: bootcamp-helpdesk-auth
  - name: bootcamp-helpdesk-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: bootcamp-helpdesk-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: bootcamp-helpdesk-db
  parameters:
    buildpack: nodejs_buildpack
- name: bootcamp-helpdesk-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: bootcamp-helpdesk-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - bootcamphelpdeskfioribootcamphelpdeskfiori.zip
      name: bootcamphelpdeskfioribootcamphelpdeskfiori
      target-path: resources/
- name: bootcamphelpdeskfioribootcamphelpdeskfiori
  type: html5
  path: app/bootcamp-helpdesk-fiori
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: bootcamp-helpdesk-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
      tenant-mode: dedicated
      xsappname: bootcamp-helpdesk-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: bootcamp-helpdesk-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: bootcamp-helpdesk-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: bootcamp-helpdesk-html5-service
    service-plan: app-host
- name: bootcamp-helpdesk-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: bootcamp-helpdesk-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
